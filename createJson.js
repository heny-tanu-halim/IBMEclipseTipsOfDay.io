//**set up walk directory framework
//**for each directory:
//**  get all html links
//**  if there is at least one html links, create index file
//**  return list of links.

var fs = require("fs");
var path = require("path");